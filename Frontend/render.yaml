services:
  - name: petcare-frontend
    type: web
    env: node
    plan: starter
    buildCommand: npm install && npm run build:ssr
    startCommand: node dist/petcare-frontend/server/server.mjs
    autoDeploy: true
    envVars:
      NODE_VERSION: 22
